interface AppConfigProps {
	appName: string;
	headerColor: string;
	headerHeight: number;
}

export const APP_CONFIG: AppConfigProps = {
	appName: "my-app",
	headerColor: "#ffaa00",
	headerHeight: 64,
};
